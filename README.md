# Swagger
**Swagger** found at http://localhost:8828/swagger-ui.html after starting application locally.

# Postman collection
You can import docs/copygram_posts_api_v1.yaml

# Run locally
- Copygram Auth service must be running
- Perform a login on Auth service to get JWT and logged in userId
- Authenticate all requests to this service using the JWT
